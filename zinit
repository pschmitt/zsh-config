# Create required directories and downlod dependencies
echo "Running zshinit ($0). You should not see this more than once"

# fzf plugin
gem install curses

# virtualenvwrapper
mkdir -p "$WORKON_HOME"

# urxvt
mkdir -p "$(dirname $RXVT_SOCKET)"

mkdir -p "$(dirname $ZINIT_LOCK)"
touch $ZINIT_LOCK
